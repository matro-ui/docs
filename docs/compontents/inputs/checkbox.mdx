---
sidebar_position: 1
title: <Checkbox/>
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import s from '../../../src/css/mdx.module.css';
import Sources from "../../../assets/components/Sources/Sources";
import PropsBlock from "../../../assets/components/Props/Props.jsx";
import I from "../../../assets/components/I";
import {Checkbox} from "matro-ui";

# Checkbox

The `<Checkbox/>` component allows to select or deselect options in a form. It provides a customizable checkbox input with various options.

<Sources 
  git="https://github.com/matro-ui/matro-ui/tree/main/dist/inputs/Checkbox"
  npmjs="https://www.npmjs.com/package/matro-ui" 
  name="checkbox"
/>


<div className={s.tabs}>
<Tabs className={s.parent}>
  <TabItem value="usage" label="Usage" attributes={{className: [s.item, s.usage]}}>
## Import 

```jsx
import { Checkbox } from "matro-ui";
```
- `Checkbox`: Checkbox input
{/*  */}

 ## Usage

 ```jsx live 
  <Checkbox>Checkbox</Checkbox>
```

 ## Disabled Checkbox

 ```jsx live 
 <Stack style={{gap: 20}}>
    <Checkbox disabled>
      Checkbox
    </Checkbox>
    <Checkbox disabled checked>
      Checkbox
    </Checkbox>
  </Stack>
```

 ## Checkbox sizes

 Pass the <code>font-size</code> to change the size of the Checkbox. Values can be either <code>px</code>, <code>em</code>, <code>rem</code> or <code>%</code>.

 ```jsx live 
 <Stack style={{gap: 20, alignItems: "flex-end"}}>
    <Checkbox style={{fontSize: "14px"}} checked>
      Checkbox
    </Checkbox>
    <Checkbox style={{fontSize: "16px"}} checked>
      Checkbox
    </Checkbox>
    <Checkbox style={{fontSize: "1.2rem"}} checked>
      Checkbox
    </Checkbox>
  </Stack>
```

 ## Checkbox with custom color

 You can override the color scheme of the <I>Checkbox</I> to any color by providing the css variable <code>--bg</code>.

 ```jsx live 
 <Stack style={{gap: 20, alignItems: "flex-end"}}>
    <Checkbox style={{"--bg": "red"}} checked>
      Checkbox
    </Checkbox>
    <Checkbox style={{"--bg": "#48b048"}} checked>
      Checkbox
    </Checkbox>
    <Checkbox style={{"--bg": "#4848d7"}} checked>
      Checkbox
    </Checkbox>
 </Stack>
```

 ## Checkbox as toggle

 Pass the <code>toggle</code> prop to change the view of the Checkbox.

 ```jsx live 
 <Stack style={{gap: 20, alignItems: "flex-end"}}>
    <Checkbox style={{fontSize: "14px"}} checked toggle>
      Checkbox
    </Checkbox>
    <Checkbox style={{fontSize: "16px"}} checked toggle>
      Checkbox
    </Checkbox>
    <Checkbox style={{fontSize: "1.2rem", "--bg": "#48b048"}} checked toggle>
      Checkbox
    </Checkbox>
 </Stack>
```

 ## Toggle Component

 Alternatively, you can use the <code>`<Toggle/>`</code> component. It wraps <code>Checkbox</code> and pass all props to it.

 ```jsx live 
  <Toggle style={{fontSize: "1.1rem"}} checked toggle>
    Toggle
  </Toggle>
```



  </TabItem>


  <TabItem value="props" label="Props" attributes={{className: [s.item, s.props]}}>
## Props

<PropsBlock payload={[
{
  title: "value",
  type: "boolean",
  desc: "The value of the checkbox."
},
{
  title: "onChange",
  type: "function",
  desc: "Callback function triggered when the checkbox value changes.",
},
{
  title: "name",
  type: "string",
  desc: "The name attribute of the checkbox.",
},
{
  title: "checked",
  type: "boolean",
  desc: "Indicates whether the checkbox is checked.",
},
{
  title: "toggle",
  type: "boolean",
  desc: "Indicates whether the checkbox is in toggle mode.",
},
{
  title: "linear",
  type: "boolean",
  desc: "Indicates whether the checkbox has a linear type of animation.",
},

]}/>

  </TabItem>



  {/* <TabItem value="extra" label="Extras" attributes={{className: [s.item, s.extra]}}>
  </TabItem> */}



</Tabs>
</div>
